# tooez
A programming language for dummies (made by a dummy)

Using plain english, no weird == or <.

A kid could probably use this

if it were completed...

## Build Instructions
### Dependencies:
- Cmake

```zsh
cmake . -DCMAKE_BUILD_TYPE=release
```

make sure you have the -DCMAKE_BUILD_TYPE=release, as it builds universal binaries if you are on macos
